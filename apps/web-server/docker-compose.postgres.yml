name: relay-server

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: seams
      POSTGRES_PASSWORD: seams
      POSTGRES_DB: seams
    ports:
      - '5432:5432'
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
