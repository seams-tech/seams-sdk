# session/restore

## Owns

Persisted-session restore coordination and restore command input/output types.

## May Import

Sealed-session persistence boundaries and shared primitive session types.

## Must Not Import

Operation flows, signing protocol entrypoints, or prompt/runtime modules.

## Entrypoints

- `restoreCoordinator.ts`
