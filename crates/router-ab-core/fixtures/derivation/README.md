# Router/A/B Derivation Fixtures

Fixtures in this directory pin JSON shape and committed public metadata before
candidate algorithms are selected.

The first committed file includes context and transcript digests for canonical
local transcript bindings. Algorithm-specific fixtures will add opened outputs,
failure cases, and refresh cases after the derivation decision gate lands.
