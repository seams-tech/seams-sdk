# Lean Boundary Track

This directory is reserved for the Rust boundary model after the production
candidate is selected.

The likely path is:

1. stabilize Rust types and vectors
2. mirror boundary enums in Lean
3. add extraction only if Aeneas gives useful coverage for the selected formula

The current files are intentionally small and model only role/output labels.
